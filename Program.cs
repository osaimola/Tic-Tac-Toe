using System;

namespace CSharpAssignment
{
    class Program
    {
        string[,] gameState = { { ".", ".", "." }, { ".", ".", "." }, { ".", ".", "." } };
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
        }
    }
}
